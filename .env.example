# ============================================================
# 环境变量模板：复制成 .env.local 后填真实值（.env.local 已被 .gitignore 忽略）
#
#   PRIVATE_POST_PASSWORD —— 私有文章（frontmatter: private: true）正文的加密口令。
#     构建期用它 AES-256 加密，浏览器端由读者输入同一口令解密；
#     口令本身不会写进源码，也不会出现在构建产物里。
#     本地：复制本文件为 .env.local 并填口令。
#     服务器：在 /var/www/blog/.env.local 里放同一份（只放一次，部署脚本不会覆盖它）。
#
#   SKIP_ENCRYPT —— 置 true 时跳过加密，私有文章以明文构建（仅本地预览用，
#     生产环境千万不要设；usertools\local-run.ps1 已自动设置，无需写在这里）。
# ============================================================
PRIVATE_POST_PASSWORD=换成你自己的口令
# SKIP_ENCRYPT=true
