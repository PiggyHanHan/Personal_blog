# 2026-08-28 日志

> 按 `tasks.md` 完成「内容收拢到 content/」改造，本地场景零验证通过，并按场景四在服务器（114.55.74.46）做了全新部署。

## 改造：内容统一收归 content/

- **PDF 附件**：`public/files/` → `content/files/`（唯一附件「分段式拓扑后处理…pdf」已移动，`public/files/` 空目录已删除）
- **代码**：`app/api/view/[...path]/route.ts`、`app/api/download/[...path]/route.ts` 的 `filesRoot` 改为 `path.join(process.cwd(), "content", "files")`，防路径穿越注释同步更新。URL 仍是 `/files/xxx.pdf`（文章 frontmatter / 正文链接），由 `/api/view`、`/api/download` 及 Nginx `/files/` 规则解析到 `content/files/`。
- **public/ 现状**：仅剩前端静态资源（`bg/`、`hutao/`、`friends/`、`border/`）＋ 构建产物 `md_images/`。
- **文档**：README.md、AGENTS.md、内容更新指南.md、启动服务器指南.md、deploy-light.ps1 全部改为「内容在 content/、PDF 在 content/files/」；deploy-light.ps1 移除已不存在的 `public/files`。

> 说明：`public/friends/`、`public/border/` 属于前端静态资源，且部署指南的 Nginx 只有 `/files/`→`content/files/` 规则、没有 `/friends/`→content 规则，故暂保留在 `public/`（友链/边框图走静态托管正常）。

## 场景零：本地验证

- `npm.cmd run build` 通过（23 个路由，`/api/view`、`/api/download` 为动态路由）
- `next start -p 80` 启动，`http://localhost` 各路由 200；`/api/view/<pdf>` 内联、`/api/download/<pdf>` 下载均正确返回 `content/files/` 中的 PDF。

## 场景四：服务器全新部署

- 打包 `deploy-full.ps1` → `blog-full.tar.gz`（含 `content/files/`，无 `public/files/`）
- 服务器 `/var/www`：备份旧 `blog` → `blog_backup_<时间戳>`，解压新包到 `blog/`，`npm install`、`npm run build`（23 路由全部生成），`pm2 restart blog` + `pm2 save`。
- Nginx：写入带 `location /files/ { alias /var/www/blog/content/files/; }` 的站点配置，`nginx -t` 通过、`systemctl reload nginx`。
- 公网验证 `http://114.55.74.46`：首页/文章/图片/`/api/view`、`/api/download`、直连 `/files/<pdf>` 全部 200。

## 备查

- 服务器：114.55.74.46（Node v22.23.2，Ubuntu，pm2 进程名 blog，端口 3000，Nginx 80→3000）
- 服务器可用 `pm2 list`、`pm2 restart blog`、`nginx -t && systemctl reload nginx`
